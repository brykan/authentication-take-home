import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.training.authentication',
  appName: 'authentication-take-home',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
